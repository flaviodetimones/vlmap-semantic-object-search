<launch>
  <!-- Use the standard ROS keyboard teleop, remapped to the official HSR
       Gazebo command topic. -->
  <arg name="cmd_vel_topic" default="/hsrb/command_velocity"/>
  <arg name="speed" default="0.25"/>
  <arg name="turn" default="0.70"/>
  <arg name="repeat_rate" default="0.0"/>
  <arg name="key_timeout" default="0.6"/>

  <node pkg="teleop_twist_keyboard"
        type="teleop_twist_keyboard.py"
        name="hsr_keyboard_teleop"
        output="screen">
    <param name="speed" value="$(arg speed)"/>
    <param name="turn" value="$(arg turn)"/>
    <param name="repeat_rate" value="$(arg repeat_rate)"/>
    <param name="key_timeout" value="$(arg key_timeout)"/>
    <remap from="/cmd_vel" to="$(arg cmd_vel_topic)"/>
    <remap from="cmd_vel" to="$(arg cmd_vel_topic)"/>
  </node>
</launch>
