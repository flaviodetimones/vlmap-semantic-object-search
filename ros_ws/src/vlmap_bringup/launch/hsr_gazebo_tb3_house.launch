<?xml version="1.0"?>
<!--
  HSR + move_base + VLMap stack on the TurtleBot3 House world
  (turtlebot3_gazebo). Small fully-furnished apartment: kitchen, living
  room, bedroom, bathroom, hallways. Apache 2.0, ROBOTIS.
-->
<launch>
  <arg name="gui" default="true"/>
  <arg name="use_rviz" default="true"/>
  <arg name="use_rosbridge" default="false"/>
  <arg name="paused" default="false"/>

  <arg name="robot_x" default="-3.0"/>
  <arg name="robot_y" default="1.0"/>
  <arg name="robot_z" default="0.05"/>
  <arg name="robot_yaw" default="0.0"/>

  <env name="GAZEBO_MODEL_PATH"
       value="$(find turtlebot3_gazebo)/models:$(optenv GAZEBO_MODEL_PATH)"/>

  <include file="$(find vlmap_bringup)/launch/hsr_gazebo_move_base.launch">
    <arg name="world" value="$(find turtlebot3_gazebo)/worlds/turtlebot3_house.world"/>
    <arg name="gui" value="$(arg gui)"/>
    <arg name="use_rviz" value="$(arg use_rviz)"/>
    <arg name="use_rosbridge" value="$(arg use_rosbridge)"/>
    <arg name="paused" value="$(arg paused)"/>
    <arg name="robot_x" value="$(arg robot_x)"/>
    <arg name="robot_y" value="$(arg robot_y)"/>
    <arg name="robot_z" value="$(arg robot_z)"/>
    <arg name="robot_yaw" value="$(arg robot_yaw)"/>
  </include>
</launch>
